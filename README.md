# NixOS config

Main repo: <https://codeberg.org/lpdkt/nichts> \
Mirror: <https://github.com/lpdkt/nichts>

## Disclaimer

This config is not optimized to be used by anyone besides myself.  
If you want to use it, make your own changes!

### stuff i use

distro: `nixos` \
wm: `niri` \
theme:`tokyo night` \
terminal emulator: `foot` \
shell: `fish + starship` \
text editor: `neovim` \
application launcher: `fuzzel` \
bar: `noctalia` \
browser: `hardened firefox` \
file manager: `yazi & thunar` \
notifications: `noctalia` \
video player: `mpv` \
image viewer: `imv` \
pdf viewer: `zathura` \
screen recording: `obs` \
music player: `mpd+rmpc` \
email: `thunderbird`
